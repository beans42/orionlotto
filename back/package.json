{
	"scripts": {
		"dev": "bun --watch index.js",
		"build": "rm -rf dist && bun build index.js --minify --target bun --packages external --outdir dist && cp package.json .env db.json dist && cd dist && bun install --frozen-lockfile",
		"start": "NODE_ENV=production bun dist/index.js"
	},
	"dependencies": {
		"axios": "^0.27.2",
		"cookie-parser": "^1.4.6",
		"cors": "^2.8.5",
		"express": "^5.1.0",
		"express-jwt": "^7.7.0",
		"jsonwebtoken": "^8.5.1",
		"serverline": "^1.6.0",
		"socket.io": "^4.8.1",
		"stellar-sdk": "^10.1.1",
		"trkl": "^2.0.1"
	}
}
